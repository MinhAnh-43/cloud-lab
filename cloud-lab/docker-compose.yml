services:
  backend:
    build: ./mern-demo/server
    container_name: mern-backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/mern_db?retryWrites=true&w=majority

  frontend:
    build: ./mern-demo/client
    container_name: mern-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
